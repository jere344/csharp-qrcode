<Window x:Class="QRGenerator_Interface.View.GenerationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QRGenerator_Interface"
        mc:Ignorable="d"
        Title="GenerationView" Height="900" Width="900">
    <Grid>
        <Viewbox Stretch="Uniform"  Margin="40">
            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- TITLE -->
                <Label Grid.Row="0">QR Code Generator</Label>

                <!--TEXTE A CONVERTIR-->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label FontSize="5" Grid.Column="0">Texte à convertir :</Label>
                    


                </Grid>

            </Grid>
        </Viewbox>
    </Grid>

</Window>
